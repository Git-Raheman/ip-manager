services:
  postgres:
    image: dockeraheman/ip-manager-db:1.0
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ipmanager
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d ipmanager" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    networks:
      - ip-manager

  backend:
    image: dockeraheman/ip-manager-backend:1.0
    environment:
      DATABASE_URL: postgres://user:password@postgres:5432/ipmanager
      PORT: 3000
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - ip-manager
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  frontend:
    image: dockeraheman/ip-manager-frontend:1.0
    networks:
      - ip-manager
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  proxy:
    image: dockeraheman/ip-manager-proxy:1.0
    ports:
      - "80:8080"
    depends_on:
      - frontend
      - backend
    networks:
      - ip-manager
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/health" ]
      interval: 60s
      timeout: 10s
      retries: 5
      start_period: 30s

networks:
  ip-manager:
    driver: bridge

volumes:
  pgdata:
